Bookly PWA - Minimal starter
- Open index.html via a local server (recommended) or host on HTTPS.
- Service worker requires HTTPS (or localhost).
- Demo features: offline caching, IndexedDB storage, camera file input, notification demo.
